#include <stdio.h>
/**
  * main - prints alphabet in lower and upper cases
  *
  * Return: always returns 0 for success
  */

int main(void)
{
	char x;
	char y;

	for(x; x <= 'a'; x++)
	{
		putchar(x);
	}
	for(y; y <= 'A'; y++)
	{
		putchar(y);
	}
	putchar('\n';)
	return (0);
}
