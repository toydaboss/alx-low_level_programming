#include <stdio.h>

/**
  * main - prints out the letters of the alphabeth
  *
  * Return: always return 0
  */

int main(void)
{
	for (int x = 'A'; x <= 'Z'; x++)
		putchar(tolower(x));
	return (0)
}
